export type InitialValues = {
  firstName: string;
  lastName: string;
  phone: string;
  email: string;
  message: string;
};

export type formItem = {
  id: string;
  name: string;
  label: string;
  type: string;
};
